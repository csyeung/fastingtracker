import SwiftUI

struct RecordTabView: View {
    var body: some View {
        Text("Record")
    }
}

struct RecordTabView_Previews: PreviewProvider {
    static var previews: some View {
        RecordTabView()
    }
}
